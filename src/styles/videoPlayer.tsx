import { makeStyles } from "@mui/styles";
// import { theme } from "./theme";

const videoPlayerStyle = makeStyles({
  mainContainer: {
    "&.Mui-box .video-js": {},
  },
});
export default videoPlayerStyle;
